[project]
name = "x32-mcp-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.0.0",
]

[project.scripts]
x32-mcp = "main:main"
x32-mcp-python = "main:main"
